# ts-rs writes app/types/generated/*.ts from the structs deriving TS. Set here
# rather than in a script so a plain `cargo test` also refreshes the bindings
# instead of dropping a stray src-tauri/bindings/ directory.
[env]
TS_RS_EXPORT_DIR = { value = "../app/types", relative = true }
